# 프로젝트 설정
PROJECT_NAME=BEHIND Backend
VERSION=1.0.0
DESCRIPTION=BEHIND 프로젝트 백엔드 API
API_V1_STR=/api/v1

# CORS 설정
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]

# 데이터베이스 설정
DATABASE_URL=postgresql://user:password@localhost/behind_db

# Redis 설정
REDIS_URL=redis://localhost:6379

# JWT 설정
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 환경 설정
ENVIRONMENT=development
DEBUG=true 