# 프로젝트 설정
PROJECT_NAME=BEHIND Backend
VERSION=1.0.0
DESCRIPTION=BEHIND 프로젝트 백엔드 API
API_V1_STR=/api/v1

# CORS 설정
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]

# Railway MySQL 환경 변수 (Railway에서 자동으로 설정됨)
# MYSQL_URL=mysql://user:password@host:port/database
# MYSQL_DATABASE=database_name
# MYSQL_PUBLIC_URL=mysql://host:port
# MYSQL_ROOT_PASSWORD=root_password
# MYSQLDATABASE=database_name
# MYSQLHOST=host
# MYSQLPASSWORD=password
# MYSQLPORT=port
# MYSQLUSER=username

# Railway MySQL 환경 변수 (Railway에서 자동으로 설정됨)
# MYSQL_URL=mysql://user:password@host:port/database
# MYSQL_DATABASE=database_name
# MYSQL_PUBLIC_URL=mysql://host:port
# MYSQL_ROOT_PASSWORD=root_password
# MYSQLDATABASE=database_name
# MYSQLHOST=host
# MYSQLPASSWORD=password
# MYSQLPORT=port
# MYSQLUSER=username

# 직접 테이블 생성 사용 (마이그레이션 대신)
# 로컬 개발용 데이터베이스 설정 (Railway 환경 변수가 없을 때 사용)
# DATABASE_URL=sqlite:///./behind.db

# Redis 설정
REDIS_URL=redis://localhost:6379

# JWT 설정
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 환경 설정
ENVIRONMENT=development
DEBUG=true 